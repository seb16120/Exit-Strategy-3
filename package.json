{
  "name": "exit-strategy-3",
  "version": "1.0.0",
  "private": true,
  "description": "Local two-player web adaptation of Exit Strategy 3",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
